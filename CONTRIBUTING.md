# 为 Commit Canvas 贡献

感谢你帮助改进这个教育工具。提交内容应保持纯客户端、容易审阅，并尊重项目的安全边界：工具不得索取 GitHub Token、自动联网、自动推送，也不得把装饰性提交包装成真实工作成果。

## 开始之前

1. 搜索现有 issue 和 pull request，避免重复工作。
2. 对较大的功能或交互变化，先开 issue 说明使用场景、设计和安全影响。
3. fork 仓库并创建主题分支；开发时只使用你拥有的专用练习仓库测试生成脚本。
4. 不要提交真实 Token、个人邮箱、运行时设计数据、生成的练习仓库或其他隐私信息。

## 开发原则

- 保持静态、客户端优先，不引入服务器或遥测。
- 所有生成操作必须由用户明确触发，脚本内容必须可查看，不得包含 `git push`。
- Bash 与 PowerShell 导出需要正确引用用户输入，避免命令注入与意外跨目录操作。
- 导出必须执行 500 个 commit 的硬上限，并在超过 200 个时清晰警告；文档和示例应推荐小图案、低强度，并遵守 GitHub Acceptable Use Policies。
- 生成脚本必须在非 Git 仓库或工作区不干净时中止。幂等保护应使用稳定提交标记，只跳过当前 `HEAD` 可达历史中的匹配标记，并准确报告实际创建数和跳过数；需要同时测试 Bash 与 PowerShell 的首次运行、完整重跑和部分失败恢复，并记录不同分支或历史被重写时不保证去重。
- 文档与测试应明确：Git 必须配置 `user.name`（未导出邮箱时还需 `user.email`），而 `git commit` 会执行已启用的 Git hooks；不要通过默认绕过 hooks 来掩盖测试环境问题。
- 保持 53 × 7 网格、键盘导航、焦点可见性和触摸操作可用。
- 面向初学者的文字要准确说明 GitHub 规则、动态颜色、重复运行和恢复风险。
- 新行为应附带相称的测试；若项目没有自动测试覆盖该路径，请在 PR 中写明手动验证步骤。

本地预览请在仓库根目录运行 Windows 上的 `npm.cmd run serve`，或 macOS/Linux 上的 `npm run serve`，然后访问 <http://127.0.0.1:4173>。不要以直接打开 `index.html` 作为验证方式。

## 提交 pull request

PR 请保持单一目的，并包含：

- 改了什么以及为什么；
- 如何验证，包括浏览器和键盘/触摸场景（如适用）；
- 对脚本安全、隐私或 GitHub 贡献规则的影响；
- 可见 UI 变化的截图（如适用）。

建议使用清晰的提交信息，例如 `fix: quote PowerShell export paths`。提交 PR 即表示你同意按项目的 [MIT License](LICENSE) 许可你的贡献。

## 报告安全问题

如果问题可能导致命令注入、凭据泄露或意外修改用户仓库，请不要在公开 issue 中放入可直接利用的敏感细节。优先使用仓库的 GitHub Security 私密报告功能；若该功能尚未启用，请只公开最小复现描述并请维护者提供私密沟通方式。
